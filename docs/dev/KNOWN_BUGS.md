Generated a bug on install script like sudo pacman -S remmina
After create a script does not allow esc key only allows esc key if apply the script, I should be able to create and save recipe without applying it.
done - when running long script no indicator that it's doing something
